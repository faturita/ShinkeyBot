// Emtpy Sketch to fix upload problem: A sketch is writing continously on the Serial port and prevents you from deploying a new sketch.
// Create by Eric Phenix
// Nov 2014

void setup() 
{ 
  delay(1000);
} 

// the loop routine runs over and over again forever:
void loop()
{ 
  delay(1000);                            
}
